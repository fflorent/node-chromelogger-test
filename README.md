Node ChromeLogger Test
======================

Just a module to test Firebug 3.0 compatibility with the ChromeLogger protocol.

Installation
------------

Just checkout that project and run `npm install`.

Run
---

Just run `node server.js`.
